{
  "name": "redis-adapter",
  "version": "1.0.0",
  "description": "",
  "main": "src/adapter.js",
  "scripts": {
    "precommit": "lint-staged",
    "prepublish": "yarn run lint && yarn run test && yarn build",
    "build": "rimraf lib && tsc",
    "lint": "tslint \"src/**/*.ts\"",
    "fix": "tslint \"src/**/*.ts\" --fix",
    "test": "jest"
  },
  "keywords": [
    "node",
    "casbin",
    "redis",
    "node-casbin",
    "redis-adapter",
    "casbin-redis"
  ],
  "author": "Node-Casbin",
  "contributor": "Vinod Kumar Nair <vinod827@gmail.com>",
  "licenses": [
    {
      "type": "Apache-2.0",
      "url": "http://www.apache.org/licenses/LICENSE-2.0"
    }
  ],
  "dependencies": {
    "casbin": "^5.0.4",
    "redis": "^3.0.2"
  }
}
